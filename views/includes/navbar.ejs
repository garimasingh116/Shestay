<nav class="navbar navbar-expand-md sticky-top shadow-sm" style="background-color: #f3edf9;">
  <div class="container-fluid">
    <!-- Brand -->
    <a class="navbar-brand fw-bold text-white px-3 py-2 rounded-3" href="/listings" style="background-color: #a66dd4;">
      <i class="fas fa-shield-alt me-2"></i> SheStay
    </a>

    <!-- Toggle for mobile -->
    <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Nav content -->
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <!-- Left nav links -->
      <div class="navbar-nav ms-3">
        <a class="nav-link text-dark fw-semibold" href="/listings">
          <i class="fas fa-compass me-1 text-primary"></i> Explore
        </a>
        <a class="nav-link text-dark fw-semibold" href="/listings/new">
          <i class="fas fa-plus-circle me-1 text-success"></i> Add Listing
        </a>
      </div>

      <!-- Right auth/user links -->
      <div class="navbar-nav ms-auto d-flex align-items-center">
        <% if (!currUser) { %>
          <a class="btn btn-outline-primary me-2 rounded-pill px-3" href="/signup">
            <i class="fas fa-user-plus me-1"></i> Sign Up
          </a>
          <a class="btn rounded-pill px-3 text-white" style="background-color: #a66dd4;" href="/login">
            <i class="fas fa-sign-in-alt me-1"></i> Log In
          </a>
        <% } else { %>
          <a class="nav-link fw-semibold text-dark" href="#">
            <i class="fas fa-user-circle me-1 text-secondary"></i> <%= currUser.username %>
          </a>
          <a class="btn btn-outline-danger ms-2 rounded-pill px-3" href="/logout">
            <i class="fas fa-sign-out-alt me-1"></i> Log Out
          </a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
